CREATE TABLE SPOTIFY_ACCOUNT_NEW (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SPOTIFY_USER_ID VARCHAR(255) NOT NULL UNIQUE,
    DISPLAY_NAME VARCHAR(255) NOT NULL,
    REFRESH_TOKEN VARCHAR(512) NOT NULL,
    CREATED_AT TIMESTAMP WITH TIME ZONE NOT NULL,
    UPDATED_AT TIMESTAMP WITH TIME ZONE NOT NULL,
    VERSION BIGINT NOT NULL DEFAULT 0
);

INSERT INTO SPOTIFY_ACCOUNT_NEW (SPOTIFY_USER_ID, DISPLAY_NAME, REFRESH_TOKEN, CREATED_AT, UPDATED_AT, VERSION)
SELECT SPOTIFY_USER_ID, DISPLAY_NAME, REFRESH_TOKEN, CREATED_AT, UPDATED_AT, VERSION
FROM SPOTIFY_ACCOUNT;

DROP TABLE SPOTIFY_ACCOUNT;

ALTER TABLE SPOTIFY_ACCOUNT_NEW RENAME TO SPOTIFY_ACCOUNT;
